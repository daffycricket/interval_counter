# Schema pointer

The authoritative schema for `design.json` is expected at `schema/minifigma.schema.json`.
Keep this file in sync with your central copy. If differing, **fail** fast and ask for alignment.

