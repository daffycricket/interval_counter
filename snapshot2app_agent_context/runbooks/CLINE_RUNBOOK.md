# Runbook – Cline

- Use the workspace root and mount `schema/`, `contracts/`, `guides/`, `prompts/`.
- Execute phases in order; persist artifacts under `reports/` and `plans/`.
- Enforce aborts on contract violations and request clarification with diffs.
